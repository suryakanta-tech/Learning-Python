1) git clone https://github.com/alokkumar1986/19th-Dec-2025.git

git clone 

To copy a git branch to local system / computer (one time)

2) git add -A 

adding files to git stag

3) git commit -m "message"

commit files to git

4) git push

To push files to branch from local 

5) git pull

To get all the branches in the git

6) git checkout origin/<branch_name>

To change the branch in local system

7) git status 

To check updates in the git

